# microservices

microservices :seedling:

## commands

```shell
# setup docker
docker compose up -d

# refresh dependencies
./gradlew --refresh-dependencies

# build
./gradlew build

# run
./gradlew bootRun
```
